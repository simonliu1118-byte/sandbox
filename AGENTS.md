# AGENTS.md

本 repository 的 AI／協作者唯一入口索引；本檔不是第四層永久規則。

開始工作前依序：

1. 讀根 `REPOSITORY_RULES.md`。
2. 讀根 `REPO_POLICY.md`。
3. 確認目標程式位於 `apps/<ProjectName>/`，並讀該專案唯一的 `PROJECT_RULES.md`。
4. 再讀 README、REQUIREMENTS、TODO、技術規格、版本紀錄等狀態／參考文件。
5. 確認目標專案目前的 `VERSION`、`BUILD`、工作 branch 與實際 source 狀態。
6. 比對本 repo `COMMON_RULES_VERSION` 與 AITeam `main`；若落後或內容有疑義，先同步共通規則再開發。

不得以舊對話、舊 branch 或先前記憶覆蓋最新 `main` 與正式三層規則，也不要自行建立新的 VERSIONING、TEAM_RULES、DEVELOPMENT_RULES、project-level AGENTS 或其他平行永久規則檔。
