# AGENTS.md

本 repository 的 AI／協作者入口。

開始工作前：

1. 先讀根目錄 `REPOSITORY_RULES.md`。
2. 確認目標程式位於 `apps/<ProjectName>/`。
3. 若該程式存在 `PROJECT_RULES.md`，再讀取該檔。
4. 修改前先確認該程式目前的 `VERSION` 與 `BUILD`。
5. 版本推進依根目錄 `REPOSITORY_RULES.md`，各程式獨立計版。

不要把 CYapps、CYapps_pvt 或其他 repository 的治理同步規則自動套用到本 repository。
