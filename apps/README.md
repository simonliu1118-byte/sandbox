# apps

每個個人程式各自建立一個獨立子目錄：

```text
apps/<ProjectName>/
```

每個可執行／可發行程式至少應有：

```text
PROJECT_RULES.md
VERSION
BUILD
```

建議新專案初始值：

```text
VERSION = 0.1.0
BUILD = 0
```

實際起始版本可依使用者要求調整；共通版本與開發規則依 repository 根 `REPOSITORY_RULES.md`。
